mod lib;

fn main() {
    println!("Hello, world!");

    // Let's create a escrow contract between two parties that want to exchange a value
    // The contract will hold the value until both parties agree to release it
}
